services:
  - type: web
    name: virucheck-api
    env: python
    buildCommand: |
      apt-get update && apt-get install -y tesseract-ocr tesseract-ocr-spa libtesseract-dev
      pip install -r requirements.txt
    startCommand: uvicorn ocr_service:app --host 0.0.0.0 --port $PORT